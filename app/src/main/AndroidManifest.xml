<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dam_proiect_var_activity">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DAM_Proiect">
        <activity android:name=".MainActivity"/>
        <activity android:name=".util.DiscountCouponsActivity"/>
        <activity android:name=".util.PartnerBankActivity"/>
        <activity android:name=".util.AddCardActivity"/>
        <activity android:name=".util.TransactionActivity"/>
        <activity android:name=".util.AddTransactionActivity"/>
        <activity android:name=".util.Register"/>
        <activity android:name=".util.InfoActivity"/>
        <activity android:name=".util.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>